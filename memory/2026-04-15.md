- Proyecto activo de continuidad: `/home/alexander-ortega/.openclaw/workspace/nexplay` en rama `production`, con trabajo operativo continuo sobre consola real de Nexplay.
- Entre 2026-04-14 noche y 2026-04-15 madrugada se empujaron estos commits a `production`: `7e1515b` (docs fuente única), `f3c149a` (UX operativa de puestos), `07e6153` (reservas alineadas con estado operativo), `daf5e95` (reconciliación reservas con sesiones), `5872269` (historial operativo por puesto), `0150bef` (KPIs operativos del dashboard), `f64d4f7` (runtime productivo backend), `fb381e1` (cierre smoke test y pulido dashboard).
- Fix productivo crítico confirmado para backend Nexplay: `backend/package.json` debe usar `start:prod = node dist/src/main.js`; el PM2 previo intentaba `dist/main` y causaba `MODULE_NOT_FOUND` + `502` público.
- Fix productivo crítico confirmado para Prisma 7 en Nexplay: backend usa `@prisma/adapter-mariadb` y `mariadb` en `backend/src/prisma/prisma.service.ts`; este ajuste quedó como runtime válido en producción.
- Flujo de despliegue confirmado: frontend y backend tienen workflows separados (`.github/workflows/deploy.yml` y `.github/workflows/deploy-backend.yml`), y el workflow backend reinicia PM2 con `--update-env`.
- Estado operativo canónico de Nexplay consolidado: `Reservation` representa planificación, `GameSession` uso real, y el estado derivado del puesto respeta prioridad `maintenance > blocked > in_use > reserved > available`; no usar `game_positions.status` como única verdad.
- Producción Nexplay validada mínimamente: frontend público responde 200 en `/nexplay/dashboard/`, `/nexplay/dashboard/positions/` y `/nexplay/reservations/`; backend público `https://nexplay-api.servialpa.com/kpis` responde 401 cuando no hay token, confirmando servicio vivo y protegido.
- Incidencia real detectada en producción: el dashboard autenticado lanzó `Internal server error` por Prisma `P2021` sobre `game_position_blocks`; el error provenía de `GET /kpis` al consultar una tabla ausente o desalineada en DB productiva.
- Resultado de reconciliación productiva parcial: al ejecutar `npx prisma db execute --file prisma/migrations/20260415035000_game_position_operational_state/migration.sql` en `/opt/nexplay/backend`, Prisma devolvió `Table 'game_position_blocks' already exists`; tras reiniciar PM2, el backend volvió a responder localmente en `127.0.0.1:3001` con 401. Queda como riesgo abierto que la reconciliación DB/migraciones en producción sigue inconsistente y requiere cierre formal.
- Documento operativo actualizado durante esta sesión: `README.md` y `docs/executable-tasks.md` en Nexplay se usan como fuente de continuidad para estado productivo, smoke test y siguientes bloques.
- Bloque 12 de pulido operativo ya iniciado en local después de `fb381e1`: `src/app/dashboard/page.tsx` y `src/app/reservations/page.tsx` ganaron refresh manual, auto refresh cada 30s, marca de última actualización y mejor legibilidad temporal; build frontend validado OK, pero este último avance aún no quedó confirmado como push en la conversación antes del flush.
