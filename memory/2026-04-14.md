## Nexplay durable summary
- Repo activo de Nexplay confirmado en `/home/alexander-ortega/.openclaw/workspace/nexplay`; existe otra copia vieja/base en `/home/alexander-ortega/.openclaw/workspace/main/nexplay`.
- Frontend y backend de Nexplay usan despliegues separados: `.github/workflows/deploy.yml` para frontend estático a GoDaddy por FTP y `.github/workflows/deploy-backend.yml` para backend al droplet por SCP/SSH. Se ajustaron triggers para que frontend corra solo por cambios de frontend y backend solo por cambios de backend/workflow backend.
- Problema operativo fuerte detectado: la base MySQL de producción quedó desalineada respecto al `schema.prisma` y al historial real de migraciones. Aparecieron faltantes como `stores.allow_unpaid_reservations`, `game_positions.platform`, `reservations.game_id`, además de tabla faltante `games`. Prisma reportó migraciones pendientes pero también drift/manual changes; se realizaron correcciones manuales en DB para destrabar operación.
- En producción se confirmó que el backend puede romperse por deploys que pisan fixes manuales del droplet. Problemas concretos observados: `start:prod` apuntando a `node dist/main` cuando el build genera `dist/src/main.js`, y necesidad de usar Prisma 7 con adapter (`@prisma/adapter-mariadb`) en `PrismaService`.
- Nuevo criterio técnico permanente para Nexplay: `Reservation` representa planificación/intención; `GameSession` representa uso real; la fuente de verdad para saber si un `GamePosition` está en uso es la existencia de una `GameSession` activa.
- Regla de estado operativo definida para `GamePosition`: prioridad `maintenance > blocked > in_use > reserved > available`. El estado visible debe ser derivado de bloqueos activos, sesiones activas, reservas activas y estado base del puesto.
- Restricción permanente de naming para Nexplay: mantener estándar existente, con tablas en plural snake_case en DB, campos snake_case (`tenant_id`, `store_id`, `created_at`, etc.), y Prisma en PascalCase/camelCase con `@map(...)` y `@@map(...)`.
- Se creó endpoint backend `/kpis` para cubrir el dashboard que antes fallaba con `Cannot GET /kpis`.
- Se implementó la nueva base operativa de puestos en backend y se empujó a GitHub:
  - commit `065619a` `feat: agregar base operativa para estado de puestos`
  - incluye `GamePositionBlock` en Prisma, `PositionStateService`, endpoint `GET /positions/:id/operational-state`, endurecimiento de `SessionsService.open()` para impedir doble sesión activa, impedir check-in en bloqueo/mantenimiento y validar reservas.
- Se implementó segundo paquete operativo y se empujó a GitHub:
  - commit `5ed3a36` `feat: agregar bloqueo de puestos y check-in operativo`
  - incluye endpoints `POST /positions/:id/block`, `PATCH /positions/:id/unblock`, `POST /positions/:positionId/check-in`, además de DTOs `SetPositionBlockDto` y `CheckInDto`.
- Se implementó tercer/cuarto paquete y se empujó a GitHub:
  - commit `8c25f81` `feat: exponer y mostrar estado operativo de puestos`
  - backend `GET /stores/:storeId/positions` devuelve shape enriquecido con `position`, `operationalState`, `activeBlock`, `activeSession`, `activeReservation`; frontend `src/app/dashboard/positions/page.tsx` se adaptó para mostrar ese estado operativo.
- Quedó preparada migración manual nueva en repo para el estado operativo de puestos: `backend/prisma/migrations/20260415035000_game_position_operational_state/migration.sql`, con `CREATE TABLE game_position_blocks` e índice `idx_game_sessions_position_status`. Prisma no pudo generar shadow DB automáticamente en ese entorno, así que se preparó manualmente.
- Pendiente crítico para continuar Nexplay: reconciliar definitivamente la DB productiva con el schema Prisma y las migraciones antes de seguir ampliando frontend/operación, luego aplicar la migración de `game_position_blocks` y después completar acciones rápidas de UI (check-in, bloquear, desbloquear, cerrar sesión).
