
## Servialpa buildout and portal ops
- Built client agent structure for Servialpa under `agents/clients/servialpa/` following the KargasPTY pattern: public orchestrator (`servialpa-public`), internal orchestrator (`servialpa-internal`), public specialists (`sales`, `services`, `projects`, `customer-support`), and internal specialists (`leads`, `ops`, `content`, `reports`, `client-success`).
- Added Servialpa documentation and operating files: `README.md`, `SYSTEM_MAP.md`, `LAUNCH_CHECKLIST.md`, `public/TESTS.md`, `public/ROUTING.md`, `internal/ROUTING.md`, and `TEST_NUMBERS.md`.
- Added SERVIALVE (Venezuela affiliate) as approved part of Servialpa messaging: may be referenced as extension of the group for Venezuela opportunities, but without promising automatic coverage.
- Test numbers documented for Servialpa in normalized E.164 format: `+50767803722`, `+50766147683`, `+50766792900`. Removed `+50766167683` from the active Servialpa test list.
- Created `agents/lab-router/` with `README.md`, `ROUTING.md`, `TESTS.md` to support one shared lab number routing between `kargaspty-public`, `servialpa-public`, and `hq-internal`.
- Updated `agents/hq/internal/AGENTS.md`, `SOUL.md`, and `MEMORY.md` to treat this HQ session as the shared lab front door: when a message clearly belongs to KargasPTY or Servialpa public, the final response should be emitted directly in that public role rather than as HQ.
- Documented an operational lab issue: queued messages arriving while the shared front door is busy may not auto-answer after the current turn ends; this was logged in HQ/internal memory and lab-router docs as a pending issue.

## Servialpa portal technical access and live changes
- Confirmed Servialpa portal is hosted on GoDaddy/cPanel and works with explicit FTPS on port 21 to `servialpa.com`.
- Valid FTP user format is `Servialpa-portal-ops@servialpa.com` (plain `Servialpa-portal-ops` failed). The FTP account was validated after routing it to the real portal directory; successful listing showed `index.html`, `styles.css`, `script.js`, `assets/`, `logo_ialabs.png`, and `servialpa-demo.html`.
- Created `agents/clients/servialpa/portal-ops/` with `AGENTS.md`, `SOUL.md`, `IDENTITY.md`, `USER.md`, `MEMORY.md`, `HEARTBEAT.md`, `TOOLS.md`, and `PLAYBOOK.md`. Delegation from `servialpa-internal` now includes `servialpa-portal-ops` for technical portal publication via cPanel/FTP.
- Important safety rule documented: do not store FTP/cPanel passwords in agent docs; credentials shared in chat should be treated as temporary and rotated.
- Live production portal edits completed on `servialpa.com`:
  - Removed the project/gallery content tied to the Pizzería Italia parking image/text.
  - Removed gallery image `assets/servialpa/trabajo-4.jpg` from the visible portal.
  - Rewrote several AI-sounding text blocks to more natural production copy.
  - Updated the brand palette in `styles.css` toward deep corporate blue + safety yellow + industrial gray.
- Remote backups created before overwrites:
  - `index.backup-before-remove-pizzeria-italia.html`
  - `index.backup-before-copy-update-services.html`
  - `index.backup-before-anti-ia-copy-pass-1.html`
  - `index.backup-before-remove-trabajo-4.html`
  - `styles.backup-before-brand-palette-update.html`
