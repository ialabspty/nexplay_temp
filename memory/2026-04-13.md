- En `nexplay`, se confirmó corrección del archivo `src/app/dashboard/page.tsx` suficiente para ejecutar `npm run build` limpio.
- Se realizó commit y push en rama `production` con commit `74dc753` y mensaje `Reparar estructura JSX del dashboard`.
- Se confirmó que no existe script `npm run deploy` en el proyecto, pero sí existe workflow `.github/workflows/deploy.yml` que despliega a GoDaddy por FTP al hacer push a `production`.
- Quedaron cambios sin trackear fuera de ese commit en `backend/scripts/`; no fueron incluidos.
